<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maxpilotto.simulazione202001">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.Home01" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.List01" />
        <activity android:name=".activities.New01" />
        <activity android:name=".activities.Mod01" />
        <activity android:name=".activities.List02" />

        <provider
            android:authorities="com.maxpilotto.simulazione202001.database.ContentProvider"
            android:name=".persistance.BookProvider"/>
    </application>

</manifest>